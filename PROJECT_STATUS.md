# DrPlaga.sh GitHub Project Status

## Current state

The GitHub profile was reorganized under the `drplagash` identity.

The old `CodeBugBox` repository remains private and is used as historical backup only.

## Public repositories

```text
drplagash/drplagash        -> public profile and project documentation
drplagash/ctf-writeups     -> CTFs, DockerLabs and vulnerable machines
drplagash/cve-lab-pocs     -> CVE analysis and lab-only PoCs
drplagash/security-tools   -> scripts, parsers and defensive utilities
drplagash/payload-analysis -> defensive payload analysis and detection notes
